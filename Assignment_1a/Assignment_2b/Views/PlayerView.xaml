<UserControl x:Class="Assignment_2b.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Assignment_2b.Views"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="150" Background="{x:Null}">
	<Grid>
		<StackPanel>

			<local:HandView DataContext="{Binding PlayerHand}"/>
			
			<Button Command="{Binding HitCommand, UpdateSourceTrigger=PropertyChanged}"/>

		</StackPanel>
	</Grid>
</UserControl>
